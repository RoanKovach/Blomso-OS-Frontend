# Architecture state

**What the system is**: Blomso OS frontend (Vite + React) and backend (AWS CDK: API Gateway HTTP API, Cognito User Pool, S3, DynamoDB). Auth via Cognito Hosted UI / Managed Login. Upload spine: init → presigned S3 PUT → complete.

**Repo memory**: Update this file (and OPS_CHANGELOG.md, NEXT_ACTIONS.md, TASK_BOARD.md) whenever we change what exists—code, IaC, or out-of-band config—so the world model stays accurate.

**Major flows**:
- **Auth**: Sign in → redirect to Cognito (login URL from `VITE_COGNITO_DOMAIN` + `/login` with client_id, response_type=token, scope=openid email, redirect_uri=app + `/auth/callback`). User signs in at Cognito; callback returns to app with `id_token` in URL fragment. Callback parses fragment, calls `setAuthToken(id_token)`, then full-page redirect to app. `User.me()` calls `GET /me` with Bearer token; backend returns identity from JWT claims.
- **Logout**: Clear token from storage, redirect to Cognito `/logout` with logout_uri = app base.
- **Upload spine (backend-path, authenticated)**:
  - Frontend `Upload` page now treats uploads as **documents** with a `documentFamily` selector (currently `soil_test` or `yield_scale_ticket`; yield lane is UI-only for now). Step 1 lets the user upload a PDF; Step 2 collects field context; Step 3 runs an async flow.
  - When `VITE_API_URL` is configured and the user is signed in, `useUploadAndParse` uses the **backend upload spine only**: `POST /functions/upload/init` (filename, contentType) → backend returns presigned PUT URL and key; client PUTs file to S3; then `POST /functions/upload/complete` (uploadId, key, filename, contentType) → backend writes a `soil_upload` record in DynamoDB keyed by user `sub` and sets `status: 'uploaded'`, then asynchronously invokes the extraction Lambda.
  - The frontend no longer claims “record saved, parsing not available”; instead, it shows that upload succeeded and that extraction will run shortly, and tells the user to track status and review in **My Records**.
- **Extraction pipeline (soil tests)**:
  - Upload-complete Lambda stores `soil_upload` with `status: 'uploaded'` and then async invokes the **extraction Lambda** (parser skeleton).
  - Extraction Lambda (Node 20/24) reads the upload record and S3 object, sets `status: 'extracting'`, writes a canonical extraction artifact JSON to S3 at `extractions/{userSub}/{uploadId}/artifact.json`, creates a DynamoDB `extraction_candidate` item keyed by `id = uploadId + '_extraction'`, and finally updates the upload record with `status: 'needs_review'`, `extractionArtifactKey`, and clears any `errorMessage`. On S3/Dynamo errors it sets `status: 'failed'` and a truncated `errorMessage`.
  - Users can also explicitly re-trigger extraction with `POST /records/{id}/extract` (HTTP API route wired to a Lambda that validates ownership and reinvokes the extraction Lambda asynchronously).
- **Extraction artifact read path**:
  - Backend exposes `GET /records/{id}` which returns a single record (upload or normalized) after verifying `userSub`.
  - Backend exposes `GET /records/{id}/extraction` which looks up the corresponding `extraction_candidate` item (`id = uploadId + '_extraction'`) and returns the extraction artifact with `soil_tests` and `parserVersion`.
  - Frontend `getExtraction(uploadId)` calls the extraction endpoint and normalizes return shape to `{ soil_tests, uploadId, parserVersion }`; if the endpoint fails or returns no `soil_tests`, it returns `{ soil_tests: [] }` so the UI shows “no data” instead of faking success.
- **Records list**: Authenticated user calls `GET /records` with Bearer token → backend returns that user’s records (Scan filtered by `userSub`): both `soil_upload` (id, type, filename, contentType, key, **status**, errorMessage?, extractionArtifactKey?, createdAt, updatedAt) and `normalized_soil_test` (id, type, userSub, sourceUploadId, extractionArtifactKey?, zone_name, test_date, soil_data, field_name, field_id, crop_type, soil_type, createdAt, updatedAt) (newest first).
- **Demo vs full mode**: Guest (not signed in) = demo mode (sessionStorage, no backend persistence for records). Signed in + `VITE_API_URL` set = full mode using the backend upload, extraction, and review pipeline.

**Deployed domains** (from out-of-band / production):
- Production app: `https://app.blomso.com`
- API: from Amplify env (e.g. `https://…execute-api.us-east-2.amazonaws.com`)
- Cognito: `https://blomso-auth.auth.us-east-2.amazoncognito.com` (Managed login; branding customized in console). User Pool has **self-sign-up enabled** (codified in backend CDK `selfSignUpEnabled: true`); Managed Login shows Create account flow.

**Environment variables** (frontend): `VITE_APP_URL`, `VITE_API_URL`, `VITE_COGNITO_DOMAIN`, `VITE_COGNITO_CLIENT_ID`, `VITE_MAPBOX_TOKEN`. See README and `.env.example`.

**Key dependencies**:
- Frontend: `User.me()` → `apiGet('/me')`; auth in `src/api/auth.js` and `src/api/client.js` (token from localStorage/sessionStorage, sent as `Authorization: Bearer <token>`).
- Backend (from repo): `GET /me` returns `{ ok, sub, username, email, authProvider: 'cognito' }` from JWT claims. `GET /records` (auth) returns `{ ok, records }` (uploads and normalized_soil_test; type-specific fields). `GET /records/{id}` (auth) returns `{ ok, record }` for one record (owner check). `POST /records/normalized` (auth) body: `sourceUploadId` (required), optional `extractionArtifactKey`, `zone_name`, `test_date`, `soil_data`, `field_name`, `field_id`, `crop_type`, `soil_type` → 201 `{ ok, record }`. `POST /records/{id}/extract` triggers extraction (202).

**Constraints / invariants**:
- Callback path must match Cognito app client callback URLs (`/auth/callback`).
- Token is returned in fragment (implicit flow).
- **SPA routing**: Hosting must serve `index.html` for all paths (e.g. `/Dashboard`, `/auth/callback`) so direct URLs and auth return work. See `docs/HOSTING.md` and `amplify-spa-rewrite.json`. App routes use PascalCase; `createPageUrl` and auth default return use `/Dashboard` (from `AUTH_RETURN_DEFAULT` in `@/utils`).
- Current `/me` does not return `full_name`; with email sign-in, Cognito `username` may be the raw `sub` (UUID).

**Codified in backend CDK** (as of Task A1 + IaC drift codification): (1) **Cognito**: Callback and logout URLs for app.blomso.com, Amplify, and localhost are default values in the User Pool Client; self-sign-up enabled. (2) **API Gateway**: HttpApi CORS is set in CDK (origins: app.blomso.com, Amplify, localhost:5173; headers: Authorization, Content-Type; methods: GET, POST, PUT, PATCH, DELETE, OPTIONS). (3) **S3 uploads bucket**: BlomsoUploadsBucket has CORS in CDK (same three origins; PUT, GET, HEAD; allowedHeaders: *). See Backend `docs/DRIFT_REPORT.md`. Future deploys will not overwrite these.

**Not codified (console-only)**: Cognito Managed Login branding and custom styling. Custom domain (app.blomso.com) is Amplify/hosting config. See OPS_CHANGELOG.md and NEXT_ACTIONS.md.

**Record model (upload / extraction / normalized)**: Source of truth is Backend `docs/RECORD_CONTRACTS.md` and `docs/schemas/record-types.json`. Single DynamoDB table: `soil_upload`, `extraction_candidate` (proposed), `normalized_soil_test` (implemented). Upload has extraction stage (extractionStatus, extractionArtifactKey, extractionError, etc.). **Normalized soil test**: Backend exposes `POST /records/normalized` (F1): one record per request; body `sourceUploadId` (required), optional extractionArtifactKey, zone_name, test_date, soil_data, field_name, field_id, crop_type, soil_type. **F2 (frontend)**: Review UI calls `saveNormalizedRecords(uploadId, tests, { extractionArtifactKey })` when upload record has extractionArtifactKey; one POST per test; on success navigate to My Records with refreshData. **My Records**: Signed-in users get `GET /records`; Soil Tests tab shows (1) upload records (soil_upload) with extractionStatus and Review/Re-run extraction, (2) saved soil tests (normalized_soil_test). Demo/guest unchanged. Practices tab still uses entity APIs (not in backend).

**Backend-path review (E1, status-driven)**: Review flow is tied to backend truth; no stub when API is configured. **Data in**: Frontend fetches record first via `getRecord(uploadId)` (GET /records/{id}); then branches on `record.extractionStatus`: `extracting` → "Extraction in progress"; `extraction_failed` → show `extractionError`; `extracted` or `extractionArtifactKey` → call `getExtraction(uploadId)` (GET /records/{id}/extraction when backend implements it). **No stub**: When API configured, `getExtraction` never returns mock data; on failure or empty artifact returns `{ soil_tests: [] }` so UI shows real state. **Review Zones ready only when real artifact has data**: Step 4 shows MultiTestReview only when `extractedTests.length > 0` from a real artifact; otherwise shows explicit messages (extracting, extraction_failed, or "no zone-level data extracted yet"). **Step 3 copy**: When backend upload only, AsyncUploadFlow shows "Upload complete" and "Extraction has been started" (no implied AI success). **Data out**: `saveNormalizedRecords(uploadId, tests, { extractionArtifactKey })` → one `POST /records/normalized` per test with optional extractionArtifactKey from upload record (backend contract). **My Records**: Soil Tests tab shows (1) Uploads table: soil_upload records with extractionStatus (and extractionError tooltip), Review and Re-run extraction; (2) Saved soil tests: normalized_soil_test list. **Entry points**: (1) Upload page: after upload complete, "Continue to Review" fetches record, then status-driven step 4. (2) My Records: "Review" → Upload with backendReviewUploadId; Upload fetches record and shows status or MultiTestReview when artifact has data.
